# 今後の開発案

まだ着手していない案のメモ。やると決めたわけではなく、思いついた時点の内容をそのまま残しておく。

---

## ① モンスターデータの取り込み

### やりたいこと

いまアプリが持っているのは **種族（純血種）38種だけ**。ここに **派生種** も入れて、
「種族を選ぶ → 派生種を選ぶ」だけで、育成計算タブの基本設定が自動で埋まるようにする。

派生種ごとに決まっている値：

7項目はすべて **モンスタータブ** に集めてある。

| 項目 | いまの入力欄 |
|---|---|
| 寿命 | モンスタータブ「成長設定 > 寿命」 |
| 成長タイプ | モンスタータブ「成長設定 > 成長タイプ」（早熟 / 持続 / 普通 / 晩成） |
| ヨイワル | モンスタータブ「成長設定 > ヨイワル」 |
| 成長適正（6種 E〜A） | モンスタータブ「各パラメータの成長適正」 |
| 初期パラメーター（6種） | モンスタータブ「初期パラメーター」 |
| ガッツ回復 | モンスタータブ「ガッツ回復速度」 |

いまは全部を毎回手で入れているので、そこが丸ごと不要になる。
入力欄が1画面にまとまっているので、「派生種を選ぶ」プルダウンもこの画面に置けばよい。

### 大変なところ

**派生種は全部で400種を超える。** 上の7項目 × 400種以上をデータとして持つことになり、
その入力がほぼ手作業になる。ここが一番の山。

### 現状のコードとの関係

- 種族の一覧は `js/data/monsters.js`（技データ）が事実上の名簿になっている。
  派生種は技データを持たないので、**名簿を技データから切り離す**必要がある。
- 個体の値は `js/store.js` の `defaultSim()`（と `defaultMon().guts`）が初期値を持っている。
  派生種を選んだら、そこへ流し込む形になる。画面は `js/monster.js`。
- 種族の選択は `js/species.js` にまとまっていて、選び直すと `species:change` が飛ぶ。
  派生種の選択もここに乗せるのが自然。
- 保存データは **種族名がキー**。派生種を足すときも、既存の種族名は絶対に変えないこと。

### 決めていないこと

- 派生種ごとに記録を分けるのか、純血種でまとめるのか（保存データの持ち方が変わる）
- 400種ぶんのデータをどこから、どうやって入れるか（手入力 / 表からの変換 / 段階的に足す）
- 全部入れるとファイルが重くなるので、アイコンと同じように遅延読み込みにするか

---

## ② 育成シミュレート（週ごとの行動）

### 調整ローテに入れたいもの（依頼された仕様。まだ一部）

1. **調整ローテの開始時点の内部数値を入力する**（→ 実装ずみ。`mon.rota.start`）

   | 内部数値 | 開始時点の初期値 |
   |---|---|
   | 体型 | -100 |
   | ヨイワル | +100 |
   | ストレス | 0 |
   | 疲労 | 0 |
   | 恐れ度 | 100 |
   | 甘え度 | 100 |

   人気（fame）は挙がっていないので、いまは入力欄を出していない。

2. **エサのデータと好き嫌い**（→ 実装ずみ）

   エサ6種を `js/data/feeds.js` に収録した。
   ストレス / 恐れ度 / 甘え度 は好き嫌いで値が変わり、体型と買値は変わらない。

   | エサ | 体型 | 買値 | 好き（ストレス/恐れ度/甘え度） | 普通 | 嫌い |
   |---|---|---|---|---|---|
   | ジャガもどき | -1 | 10 | 0 / 0 / +1 | +4 / +3 / -4 | +16 / +4 / -10 |
   | ミルクもどき | +1 | 50 | -3 / 0 / +3 | -2 / 0 / +1 | +4 / -4 / -4 |
   | サカナもどき | +2 | 100 | -6 / 0 / +3 | -3 / 0 / +2 | +2 / +1 / -2 |
   | ゼリーもどき | -3 | 150 | -7 / +1 / +2 | -5 / +1 / +1 | +1 / +1 / -1 |
   | ニクもどき | +6 | 300 | -8 / 0 / +6 | -6 / 0 / +4 | 0 / +1 / +1 |
   | ビタミンもどき | +3 | 500 | -15 / +2 / +6 | -13 / +2 / +1 | -10 / +2 / -1 |

   どのエサが好きかはモンスターごとに違うので、**ユーザーに入力してもらう**。
   入力欄はモンスタータブ（個体の決まった値なので）。保存先は `mon.feedLike`
   （エサ名がキー。`'like'` / `'normal'` / `'dislike'`。初期値は `'normal'`）。
   調整ローテ側には、その好き嫌いを当てはめた効果を読み取り専用の表で出してある。

3. **毎週のアイテムと行動、毎月のエサを入力する**（→ 画面と計算は実装ずみ。
   行動の効果だけ未対応）

   決まったこと:
   - エサは**月初めに1回**。そのあとに**双子の水差し**の処理が起こる。
     そのあとが、その週のアイテムと行動（`js/simulator/rota-calc.js` の `simulate()`）
   - 1週ぶん入力したら次の週の欄が出てくる。**上限なし**
   - 1か月は4週。調整ローテの1週目を月初めとして数える

4. **行動のデータと体調値**（→ 実装ずみ。`js/data/acts.js`）

   | 行動 | 疲労 | ストレス |
   |---|---|---|
   | 軽トレーニング | +10 | +5 |
   | 重トレーニング | +15 | +12 |
   | 修行（1週ぶん） | +18 | +7 |
   | 冒険（4週の合計） | +70 | 0 |

   修行は4週で 疲労+72 / ストレス+28 になり、もとの表の「修行合計」と一致する。

   **修行と冒険は4週まとめて出かける。**
   1週のなかの順番は「月初めならエサ → 双子の水差し → アイテム → 行動」で、
   出かける週にはエサが間に合う。冒険だけはアイテムが使えない。
   2〜4週目はエサもアイテムも無し。帰ってきた週からはふつうに戻る。

   - 修行 … 疲労+18 / ストレス+7 が毎週たまる。体調値も毎週出る
   - 冒険 … 出ているあいだは何も起きず、体調値も出さない。
     疲労+70 は合計で、**帰ってきた週**（第1週に出れば翌月の第1週）にまとめて乗る

   月初めに出かけている途中だと、その月はエサをあげられない
   （修行を第1週から始めたときだけ、帰ってすぐにエサをあげられる）。

   **休養** … 成長段階ごとに幅のある値だが、**いちばん減らない側（最低値）**を使う。
   体型は段階に関係なく +5。甘え度も 0〜+6 の幅があるので、同じ考えで 0（動かさない）。

   | 段階 | 疲労 | ストレス |  | 段階 | 疲労 | ストレス |
   |---|---|---|---|---|---|---|
   | 第1段階 | -36 | -5 | | 準ピーク | -44 | -9 |
   | 第2段階 | -38 | -5 | | 第5段階 | -40 | -5 |
   | 第3段階 | -40 | -5 | | 第6段階 | -32 | -5 |
   | 第4段階 | -42 | -7 | | 第7段階 | -32 | -5 |
   | ピーク | -44 | -9 | | 第8段階 | -32 | -5 |

   **大会** … 疲労は体型で変わる。ストレスは結果に関係なく**大会後は0**。

   | 結果 | 疲労 |
   |---|---|
   | 優勝 | +30 + 切り捨て(体型/13) |
   | 他 | +40 + 切り捨て(体型/10) |
   | ビリ | +50 + 切り捨て(体型/8) |

   もとの目安表（優勝時に 超激ガリ+23 / 中間+30 / 超激デブ+37）と一致することを
   `test/calc.test.mjs` で確かめている。

   **体調値** … 疲労 + ストレス×2。各週の値を出す。

5. **寿命の減り方**（→ 実装ずみ）

   寿命の減りは2本立てで、どちらも寿命減少。

   | | 中身 | 暦 |
   |---|---|---|
   | **A 週経過ぶん** | 1週たてば、それだけで寿命が1週減る | 進む |
   | **B 追加ぶん** | 週経過に「加えて」かかるぶん | 進まない |

   B は **体調値ぶんとイベントぶんの両方**がかかる。

   体調値の表は **★週経過ぶんを含んだ1週ぶんの合計★**。
   体調値が69以下なら -1 で、減るのは週経過ぶんだけ（体調値ぶんの追加は無い）。
   追加ぶんは「表の値 − 週経過ぶん」で出す。

   | 体調値 | 合計 | 追加ぶん |  | 体調値 | 合計 | 追加ぶん |
   |---|---|---|---|---|---|---|
   | 〜69 | -1 | 0 | | 175〜209 | -5 | -4 |
   | 70〜104 | -2 | -1 | | 210〜244 | -6 | -5 |
   | 105〜139 | -3 | -2 | | 245〜269 | -7 | -6 |
   | 140〜174 | -4 | -3 | | 270〜 | -8 | -7 |

   イベントごとの決まりごと。疲労0・ストレス0から始めた場合は
   どれも育成計画の `EV_COST` とぴったり合う。

   | イベント | A 週経過 | B 体調値ぶん | B イベントぶん | 合計 | `EV_COST` |
   |---|---|---|---|---|---|
   | 大会 | 1 | 0（体調30なので） | -3 | -4 | 4 ✓ |
   | 修行 | 4 | 毎週（0/0/-1/-2） | なし | -7 | 7 ✓ |
   | 冒険 | **0** | 出さない | -2 | -2 | 2 ✓ |

   - **大会** … 出場だけで -3 がベース。
     ただし**大会が終わった時点の疲労が70を越えるとベースが倍の -6** になる。
     大会後はストレス0なので、このときの体調値は疲労と同じ数になる。
     疲労がたまった状態で出ると 週経過1 + 体調値ぶん + ベース6 で -8 以上になり、
     もとの資料の「実際は体調値による寿命減少も合わせると-8週以上」と合う。
     疲労は全快で出るのがよい、という話につながる
   - **修行** … 修行そのものの追加は無く、毎週の体調値ぶんだけ。
     疲労0・ストレス0から出れば 体調値が 32 / 64 / 96 / 128 と上がり、
     追加は 0 / 0 / -1 / -2。週経過4と合わせて **-7**
   - **冒険** … 暦は4週進むのに**週経過を寿命に数えない**（特別扱い）。
     出ているあいだは体調値を出さないので、冒険ぶんの -2 だけ

   `test/calc.test.mjs` で `EV_COST` と突き合わせてある。

   決めたこと:
   - 双子の水差しの所持数は調整ローテ全体で1つの値（途中の増減は考えない）
   - 人気（fame）は追わない
   - 休養に使う成長段階は、調整ローテ全体で1つ選ぶ（`mon.rota.stage`）

6. **リセットと保存**（→ 実装ずみ）

   調整ローテのいちばん下に2つのボタンを置いた。

   - **リセット** … `mon.rota` を `defaultRota()` で作り直す。
     開始時点の内部数値・双子の水差し・成長段階・エサ・アイテム・行動が全部戻る
   - **早見のローテに保存** … 組んだ内容を写して `state.rotas` に足し、
     早見タブの「ローテ」の箱に並べる（`js/reference/reference.js`）。
     残すのは **エサ・アイテム・行動** と、**最初の状態・最後の状態**（内部数値全部）。
     写したあとは計算し直さないので、調整ローテを組み替えても動かない

   `state.rotas` は種族ごとではなく全体で1つ持つ（早見タブは種族に関係しないため）。
   どの種族のものかは、写すときに `species` として一緒に残している。
   成長段階と双子の水差しの所持数は写していない（依頼された中身に入っていないため）。

   決めていないこと:
   - 保存したローテに名前を付けられるようにするか（いまは種族名と保存日時だけ）
   - 保存したローテを調整ローテに読み戻せるようにするか

   決めたこと（つづき）:
   - 入力欄は寿命の減り方に関係なく 1週 → 2週 → 3週 → 4週 → 翌月 と進む（暦どおり）
   - 冒険の 疲労+70 は4週ぶんの合計。帰ってきた週にまとめて乗せる
   - 修行は1週ずつ選ぶ形のまま（4週ぶん選ぶと合計になる）

   まだ決めていないこと:
   - 月初めに冒険で出かけていると、その月はエサを選べない（欄が「—」になる）。
     この扱いでよいか
   - 育成計画と調整ローテの受け渡し（調整ローテで組んだ期間を計画に反映するか）

> **進行中**：アイテムデータ16件を `js/data/items.js` に数値つきで収録した。
> 内部数値の範囲と割合の切り捨ては `js/simulator/inner-calc.js`。
> 名前と効果の一覧は早見タブの「アイテム」に一本化してある（`js/reference/reference.js`）。
> 育成計算タブの **「調整ローテ」**（`js/simulator/rotation.js`）には、
> 開始時点の内部数値の入力だけ入れてある。保存先は `mon.rota`
> （`start` / `weeks` / `feeds`。weeks と feeds は形をまだ決めていない空配列）。
> まだ無いのは、週ごとに行動を並べて追いかける画面と計算そのもの。
> なおユーザーはアイテムの効果を覚えている前提で、画面には出さない方針
> （思い出したいときは早見タブを見る）。

### 決まっていること（確認ずみ）

**内部数値の範囲**（生まれた直後の値はヨイワル以外すべて 0）

※ 調整ローテの「開始時点の初期値」（体型-100 / 恐れ度100 / 甘え度100 など）は
これとは別もの。生まれた直後ではなく、調整ローテを始める時点の想定値。
コード上も `INNER.init`（生まれた直後）と `defaultRota().start`（調整ローテの開始時点）で
分けてある。

| 内部数値 | 範囲 |
|---|---|
| 疲労 / ストレス / 恐れ度 / 甘え度 / 人気 | 0 〜 100 |
| 体型 | -100 〜 100 |
| ヨイワル | -100 〜 100 |

ヨイワルだけは初期値がモンスターごとに違い、さらに **初期値から±100までしか動かない**。
初期値 -90 のモンスターなら -100〜+10 が動ける範囲（`moralRange()`）。
基本設定の入力欄も -100〜100 を5きざみで選ぶ形にした。

**計算の決まりごと**

- ゲーム内の計算は、途中で小数点以下を切り捨てる。
  「ストレス-50%」のような割合は、**割合を出した時点で切り捨て、そのあとで足し引き**する
  （ストレス75なら 37.5→37 を引いて 38。`pctDelta()`）。
- 寿命の増減は、**寿命そのものが減るのではなく、そのぶん歳を取る**（週が進む）。
  データでは `agePlus`（進む週数）として持っている。
- 1週間に **アイテム1個と行動1回の両方** ができる。アイテムは与えなくてもよい。
- イベントは、**減る寿命と実際に進む週数が違う**。

  | イベント | 寿命 | 進む週 |
  |---|---|---|
  | 大会 | -4 | 1週 |
  | 修行 | -7 | 4週 |
  | 冒険 | -2 | 4週 |

  `js/data/growth.js` の `EV_COST`（寿命）と `EV_WEEKS`（進む週）。
  育成計画の割当週から引いているのは `EV_COST` のほう。
- 寿命の消費が段階の週数を超えたら、**はみ出たぶんは次の段階から引く**。
  例: 1段階10週にパラドクシン(-18)を使うと 1段階のトレーニングは0週になり、
  次の段階が8週ぶん短くなる。
- アイテムには2種類ある。
  - 使うと無くなるもの（`kind:'use'`）… 16件のうち15件
  - 持っているだけで効くもの（`kind:'hold'`）… 双子の水差し。
    月初めに効果がかかり、**持っている数だけ重なる**（3個なら ストレス-3 / 恐れ度+3）
- 効果の続くもの（トロロン・パラドクシン）は、**その週数をかけて上がり、そのあとも残る**。
  パラドクシンの「回避と丈夫さ-10%」は、その4週のあいだ毎週、そのときの値に対してかかる。
- 種族条件つきの効果はその効果だけにかかる。
  オイリーオイルの疲労-28 / 恐れ度+1 / 甘え度+1 は全種族、ストレス-20% だけが
  デュラハン・アローヘッド・ヘンガー限定。

### まだ決めていないこと

- モンスターごとのヨイワルの初期値（①のデータが入れば一緒に入る）
- 3か月ぶんを入力する画面の形（表 / カレンダー / 週ごとのカード）
- 育成計画とシミュレートの受け渡し（シミュレートで組んだ期間を計画に反映するかどうか）

### 改良案：寿命が減るアイテムのセットを自動で作る

まだ着手していない案。実際の使い方はこうなる（パラドクシン・トロロン）。

```
 ○月1週  パラドクシンを使う → その月は4回トレーニング
 翌月1週  2個目を使う       → その月も4回トレーニング
 以下同様
```

これを育成計画の形に直すと、1個ぶんが2つのセットになる。

| | 割当週 | 中身 | トレ | 暦 |
|---|---|---|---|---|
| スルー用のセット | 18（＝寿命の減少分） | パラドクシン1個 | 0週 | 0週 |
| トレーニングのセット | 4 | 重トレ/軽トレ | 4週 | 4週 |

寿命は22週進むが、暦は4週（1か月）しか進まない。これを個数ぶん繰り返す。

**いまの作りでも、この形は手で組める。** そして「18＋4のセットを4組」と
「88週のセット1つにパラドクシン4個」は、寿命・暦・トレーニング週・上昇値が
すべて一致することを確かめてある（`js/simulator/growth-calc.js` の計算どおり）。
なので合計だけ見るなら今のままでよく、改良の中身は次の2点になる。

- セットを手で8つ作る手間をなくす（個数を入れたら、その組を自動で並べる）
- 「スルー → その月にトレーニング」という並び順が画面に出るので、
  月ごとの流れが見えるようになる。薬を使った月だけトレーニングを変える、
  といった組み方もできる

決めていないこと:

- 自動で組んだセットを、あとから手で直せるようにするか
- 割当週が「（寿命の減少分＋4）×個数」で割り切れないときの端数をどうするか
- トロロン（-6）だと1個ぶんが 6＋4＝10週。この形でよいか

### 役割の分け方

- **育成計画** … 全期間をさっくり組む。段階ごとにトレーニングとイベント、
  寿命が減るアイテムの回数を入れる。暦のずれもここで織り込む。
- **調整ローテ** … 短い期間だけ、アイテムと行動を1週ずつ細かく入れる。
  育成の最初と最後に特別なプランを立てることが多いので、そこ用。
- **早見タブのアイテム** … 名前と効果を思い出すための一覧。計算には使わない。
- トレーニングと休養の効果（アイテム以外の行動ぶんのデータ）

### やりたいこと

アイテム・トレーニング・休養といった **行動ごとの数値をアプリに覚えさせて**、
「いつ何をしたら内部数値がどうなるか」を追えるようにする。

入力のイメージ：

```
 4月1週  トレーニング：重り引き
 4月2週  アイテム：まずいマタタビ
 4月3週  休養
 4月4週  大会
 …（3か月ぶんくらい）
```

これを並べて、各タイミングの **体調値** を計算し、最終的な細かい数値を自動で出す。

### 大変なところ

計算そのものは、アイテムと行動による内部数値の変化がおおよそ決まっているので、たぶん重くない。
**アプリに覚えさせる情報の量**（アイテム一覧とその効果、行動ごとの変化）が多いのが本番。

### 現状のコードとの関係

- 育成計画は **段階ごとにまとめて計算**する作り（`js/simulator/growth-calc.js`）。
  週ごとのシミュレートは粒度が違うので、**別の計算ファイル**として足すのがよさそう。
  `growth-calc.js` と同じく、画面に依存しない純粋な計算にしておけば Node だけでテストできる。
- **開始時期（月・週）** の入力欄は育成計画のいちばん上にある。
  いまは段階が始まる時期（○月◎週）の計算に使っていて、週ごとのシミュレートの起点にもなる。
- 画面は `js/simulator/rotation.js`（いまは案内文だけ）。`#rotationArea` に描く。
  保存先はまだ決めていないが、種族ごとなら `mon.sim` の下、
  期間を複数持つなら配列にすることになる。

### 決めていないこと（つづき）

- ①のデータ（成長適正など）が入っていないと精度が出ないなら、①が先になる
