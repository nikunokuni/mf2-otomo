/* ===========================================================
   早見タブに表示する内容
   -----------------------------------------------------------
   アプリ側で決まっている（ユーザーが編集しない）データはここ。
   どれも { name, detail } の配列。name は左、detail は右に出る。
   detail は省略できる。
     例: { name: '4月4週 ブリーダー公認大会', detail: 'ランクE / 出場自由' }
   中身が空の箱は「これから追加します」と表示される。
   =========================================================== */

/** ローテ（大会の巡り） */
export const ROTATION = [];

/** アイテム */
export const ITEMS = [];

/** 合体素材 */
export const COMBI = [];
